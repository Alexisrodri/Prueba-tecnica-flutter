import 'package:flutter/material.dart';

class CardsBandlist extends StatelessWidget {
  const CardsBandlist({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hola Mundo'),
      ),
    );
  }
}
